import React from 'react'

const ShowHop = (props) => {

    const sky=[
        props.yearSky,
        props.monthSky,
        props.daySky,
        props.timeSky
    ]

    const ground=[
        props.yearGround,
        props.monthGround,
        props.dayGround,
        props.timeGround
    ]

  return (
    <div>ShowHop</div>
  )
}

export default ShowHop